# 工单：2026-06-22 Actions 扩量与排查

## 一、你这次提出的问题

1. `actions` 页面公开条目太少，不能只依赖当前 `UN AI Resource Hub`。
2. 你不认可把 `review queue` 作为对外页面重点，你最终要的是尽量自动抓取和自动展示。
3. 你注意到 `UN AI Resource Hub` 近半年不应只显示 2 条，要求我排查是不是抓漏了。
4. 你想知道 `resource` 页面当前到底是读哪个文件生成的。

## 二、这次排查后的结论

### 1. `UN AI Resource Hub` 只显示 2 条，不是前端截断，而是当前公开页面本身就只有 2 个可抓取卡片

本次我重新直接读取了：

- [https://unaihub.aiforgood.itu.int/activities.php](https://unaihub.aiforgood.itu.int/activities.php)

并按页面结构抓取 `activity-card`。结果当前公开 HTML 中实际只有 2 个卡片：

1. `Artificial Intelligence & Machine Learning Capacity Building`
2. `UN General Assembly report of the Special Rapporteur on the Right to Privacy ...`

所以，之前页面里只有 2 条，不是前端漏渲染，而是这个公开页当前可直接抓到的结构化条目确实只有 2 条。

### 2. 原来开放发现层抓不到更多条目的根因，不是“没有数据”，而是“抓到了 RSS，但在中转链接处被丢弃了”

原先脚本的逻辑是：

1. 从 `Google News RSS` 抓候选。
2. 再尝试把每条 `news.google.com/rss/articles/...` 中转链接解析成最终文章链接。
3. 如果最终还是 `news.google.com`，就当成无效来源直接丢掉。

这会导致一个问题：

- 其实 RSS 里已经给了 `source` 元数据，包含来源名称和来源官网 URL。
- 但旧逻辑过度依赖“必须成功跳转到最终文章页”，于是很多本来属于 `ITU / UNESCO / CoE / OECD.AI` 的官方来源候选，在中转层就被整批扔掉了。

也就是说，原来不是“没搜到”，而是“搜到了但没能稳定入库”。

## 三、这次已经完成的修复

### 1. 修复了开放发现层的主链路

我已修改：

- [C:\Users\14916\Desktop\ioaicollection\ai-io-site\scripts\build_actions.py](C:\Users\14916\Desktop\ioaicollection\ai-io-site\scripts\build_actions.py)

核心调整：

1. 不再把 `Google News RSS` 候选一律建立在“必须成功解析最终文章 URL”之上。
2. 改为优先使用 RSS 自带的 `<source url="...">` 官方来源元数据来判断是不是来自目标国际组织官网体系。
3. 把 `AI`、`action`、`reject` 词识别改成带边界的判断，避免把普通单词里的 `ai` 误识别成 AI 线索。
4. 新增了更严格的“公开发布门槛”：
   - 不是只要像 AI 新闻就公开；
   - 必须更像“国际组织自身行动”才公开；
   - 不够稳的先留在内部候选，不在前端直接展示。

### 2. 对 `OECD.AI` 做了更保守处理

排查中发现：

- `OECD.AI Policy Observatory` 当前会聚合大量全球 AI 相关新闻。
- 这类内容并不都属于“国际组织自身采取的 AI 行动”。

所以这次我没有粗暴放大 `OECD.AI` 的公开展示，而是收紧规则：

- 对明显偏“AI 泛新闻”的条目，不直接公开展示；
- 保留为内部候选，避免污染你的行动页。

这符合你前面特别强调的要求：不要靠硬编码乱匹配，更不要把不该收的东西当成“国际组织 AI 行动”公开放出。

### 3. 重新生成了前端数据文件

已重新生成：

- [C:\Users\14916\Desktop\ioaicollection\ai-io-site\data\actions.json](C:\Users\14916\Desktop\ioaicollection\ai-io-site\data\actions.json)
- [C:\Users\14916\Desktop\ioaicollection\ai-io-site\data\review-queue.json](C:\Users\14916\Desktop\ioaicollection\ai-io-site\data\review-queue.json)
- [C:\Users\14916\Desktop\ioaicollection\ai-io-site\data\source-registry.json](C:\Users\14916\Desktop\ioaicollection\ai-io-site\data\source-registry.json)
- [C:\Users\14916\Desktop\ioaicollection\ai-io-site\data\pipeline-status.json](C:\Users\14916\Desktop\ioaicollection\ai-io-site\data\pipeline-status.json)

## 四、这次修复后的结果

本轮重跑后：

- 公开展示行动：`11` 条
- 内部候选：`15` 条
- 已接入来源：`6` 个
- 时间范围：近 `180` 天

当前前端公开展示的来源分布：

1. `联合国 AI Resource Hub`：2 条
2. `欧洲委员会 AI 页面发现`：5 条
3. `UNESCO AI 页面发现`：3 条
4. `ITU / AI for Good 页面发现`：1 条
5. `OECD.AI 官方站发现`：0 条公开展示，保守留在候选层

这说明现在已经不是“只靠 UN AI Hub 的 2 条”了。

## 五、`resource` 页面现在读的是哪个文件

`resource` 页面当前直接读取的是：

- [C:\Users\14916\Desktop\ioaicollection\ai-io-site\data\source-registry.json](C:\Users\14916\Desktop\ioaicollection\ai-io-site\data\source-registry.json)

前端加载位置在：

- [C:\Users\14916\Desktop\ioaicollection\ai-io-site\app.js](C:\Users\14916\Desktop\ioaicollection\ai-io-site\app.js)

其中：

- `source-registry.json` 是“资源页实际展示数据”
- `action-sources.json` 是“来源配置清单”
- `build_actions.py` 是“把来源配置跑成前端数据”的生成脚本

对应文件分别是：

- [C:\Users\14916\Desktop\ioaicollection\ai-io-site\config\action-sources.json](C:\Users\14916\Desktop\ioaicollection\ai-io-site\config\action-sources.json)
- [C:\Users\14916\Desktop\ioaicollection\ai-io-site\scripts\build_actions.py](C:\Users\14916\Desktop\ioaicollection\ai-io-site\scripts\build_actions.py)

## 六、关于你不认可 `review queue` 页面这件事

我已按你的意思处理：

1. 这次没有新增任何对外的 `review queue` 页面。
2. 现在保留 `review-queue.json` 只是作为内部缓冲文件，不进入当前对外导航。
3. 对外仍然只有：
   - `机构`
   - `行动`
   - `资源`

也就是说，`review` 现在只是后台数据层，不是产品主界面。

## 七、我对当前自动化链路的判断

现在这条链路已经比上一版明显更接近“可自动跑”的状态，但还没有到“完全不需要继续加精度”的程度。

当前最大剩余问题有两个：

### 1. 一部分开放发现条目的公开链接，目前还是来源主页，而不是精确文章页

原因不是我故意偷懒，而是：

- `Google News RSS` 稳定提供了标题、发布时间、来源名、来源官网；
- 但它不稳定提供可直接拿到的最终原文 URL；
- 如果继续强行逐条跳转解析，旧问题就会回来：慢、容易超时、还会掉数据。

所以这一轮我优先保证：

- 先把“候选能稳定进来、不会整批丢失”解决掉；
- 再把“每条都能落到精确原文 URL”作为下一阶段增强。

### 2. `OECD.AI` 这类“官方站内含新闻聚合”的来源，必须继续做更细的来源拆分

这类来源不能简单按“域名正确”就直接公开。

下一步更稳的做法是：

1. 不继续只用 `site:oecd.ai` 这种泛检索。
2. 改成更细粒度地拆官方栏目。
3. 优先接：
   - 官方新闻栏目
   - 官方报告栏目
   - 官方政策/框架栏目
   - 官方活动栏目

这样才能把“组织自身行动”与“组织转载/聚合的外部 AI 新闻”分开。

## 八、这轮你现在可以直接看的本地预览

当前本地预览地址：

- [http://127.0.0.1:4175/ai-io-site/](http://127.0.0.1:4175/ai-io-site/)
- [http://127.0.0.1:4175/ai-io-site/?view=actions](http://127.0.0.1:4175/ai-io-site/?view=actions)
- [http://127.0.0.1:4175/ai-io-site/?view=sources](http://127.0.0.1:4175/ai-io-site/?view=sources)

## 九、这轮自检结论

1. 已确认 `UN AI Resource Hub` 当前公开结构化页确实只有 2 个卡片。
2. 已确认原先开放发现层的主问题是 `Google News` 中转链接过滤过严。
3. 已完成脚本修复并重跑。
4. 已确认前端数据文件已更新。
5. 已确认资源页数据源文件是 `source-registry.json`。
6. 本文档使用 UTF-8 写入，后续我会再做一次带 BOM 的编码覆盖，尽量避免你本地再次看到乱码。
